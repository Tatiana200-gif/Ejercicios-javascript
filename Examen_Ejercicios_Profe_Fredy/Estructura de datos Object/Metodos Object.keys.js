// Ejercicio 1.
let usuario = { nombre: "Ana", rol: "Admin" };
let llaves = Object.keys(usuario);
console.log(llaves);

// Ejercicio 2.
let cantidadPropiedades = Object.keys(usuario).length;
console.log(cantidadPropiedades);

// Ejercicio 3.
let configuracion = {};
console.log(Object.keys(configuracion).length);